This repo is for a Java based MapReduce framework which works using a servlet based front end.


Instructions for building and running the project?
  build using sudo
  master url - http://ip:port/master/status
  worker url - http://ip:port/worker/
  change worker port by editing the selfport param in web.xml

